<UserControl x:Class="FlatPatternExporter.TextWithFxIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FlatPatternExporter"
             x:Name="Root"
             MinHeight="0" MinWidth="0">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <TextBlock Text="{Binding ElementName=Root, Path=Text}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Style="{StaticResource CenteredCellStyle}"
                   TextTrimming="CharacterEllipsis"
                   Margin="{StaticResource RightSmallMarginThickness}"/>

        <TextBlock Style="{StaticResource FxIndicatorByTagStyle}"
                   Tag="{Binding ElementName=Root, Path=IsExpression}"/>
    </Grid>
</UserControl>
