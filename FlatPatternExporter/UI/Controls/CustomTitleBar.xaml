<UserControl x:Class="FlatPatternExporter.UI.Controls.CustomTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="TitleBarControl"
             Height="32">

    <UserControl.Resources>
        <!-- Fade Animations -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
        </Storyboard>

        <!-- Base style for update icons -->
        <Style x:Key="UpdateIconBaseStyle" TargetType="Path">
            <Setter Property="StrokeThickness" Value="1.5"/>
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="Opacity" Value="0"/>
        </Style>

        <!-- Style for the 'UpdateAvailable' icon -->
        <Style x:Key="UpdateAvailableIconStyle" TargetType="Path" BasedOn="{StaticResource UpdateIconBaseStyle}">
            <Setter Property="Data" Value="{StaticResource DownloadIconGeometry}"/>
            <Setter Property="Stroke" Value="{DynamicResource SuccessBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=TitleBarControl, Path=UpdateState}" Value="UpdateAvailable">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard Storyboard="{StaticResource FadeOut}"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Style for the 'UpToDate' icon -->
        <Style x:Key="UpToDateIconStyle" TargetType="Path" BasedOn="{StaticResource UpdateIconBaseStyle}">
            <Setter Property="Data" Value="{StaticResource CheckmarkIconGeometry}"/>
            <Setter Property="Stroke" Value="{DynamicResource SuccessBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=TitleBarControl, Path=UpdateState}" Value="UpToDate">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard Storyboard="{StaticResource FadeOut}"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Style for the 'Error' icon -->
        <Style x:Key="ErrorIconStyle" TargetType="Path" BasedOn="{StaticResource UpdateIconBaseStyle}">
            <Setter Property="Data" Value="{StaticResource ErrorIconGeometry}"/>
            <Setter Property="Stroke" Value="{DynamicResource ErrorBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=TitleBarControl, Path=UpdateState}" Value="Error">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard Storyboard="{StaticResource FadeOut}"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Window chrome button style -->
        <Style x:Key="WindowChromeButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Close button style -->
        <Style x:Key="WindowChromeCloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource WindowChromeButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ErrorBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource BackgroundSecondaryBrush}"
            BorderBrush="{DynamicResource BorderPrimaryBrush}"
            BorderThickness="0,0,0,1">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Window icon -->
            <Image Grid.Column="0"
                   Source="{Binding ElementName=TitleBarControl, Path=Icon}"
                   Width="16"
                   Height="16"
                   Margin="10,0,8,0"
                   VerticalAlignment="Center">
                <Image.Style>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <Trigger Property="Source" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>

            <!-- Window title -->
            <TextBlock Grid.Column="1"
                       x:Name="TitleTextBlock"
                       Text="{Binding ElementName=TitleBarControl, Path=Title}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="{DynamicResource StandardFontSize}"
                       FontWeight="Normal"
                       VerticalAlignment="Center"
                       HorizontalAlignment="{Binding ElementName=TitleBarControl, Path=TitleAlignment}"
                       TextTrimming="CharacterEllipsis">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Margin" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=TitleBarControl, Path=Icon}" Value="{x:Null}">
                                <Setter Property="Margin" Value="10,0,0,0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Content area for custom controls -->
            <ContentPresenter Grid.Column="2"
                              Content="{Binding ElementName=TitleBarControl, Path=ContentArea}"
                              VerticalAlignment="Center"/>

            <!-- Window buttons -->
            <StackPanel Grid.Column="3"
                        Orientation="Horizontal">

                <!-- Update button with conditional space reservation -->
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Width" Value="Auto"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=TitleBarControl, Path=ReserveUpdateButtonSpace}" Value="True">
                                    <Setter Property="Width" Value="46"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Button x:Name="UpdateButton"
                            Style="{StaticResource WindowChromeButtonStyle}"
                            Width="46"
                            Height="32"
                            Click="UpdateButton_Click"
                            ToolTip="{Binding ElementName=TitleBarControl, Path=UpdateTooltip}">
                        <Viewbox Width="16" Height="16">
                            <Grid>
                                <Path Style="{StaticResource UpdateAvailableIconStyle}"/>
                                <Path Style="{StaticResource UpToDateIconStyle}"/>
                                <Path Style="{StaticResource ErrorIconStyle}"/>
                            </Grid>
                        </Viewbox>
                    </Button>
                </Grid>

                <!-- Minimize button -->
                <Button x:Name="MinimizeButton"
                        Style="{StaticResource WindowChromeButtonStyle}"
                        Width="46"
                        Height="32"
                        Click="MinimizeButton_Click">
                    <Path Data="M0,5 L10,5"
                          Stroke="{DynamicResource TextSecondaryBrush}"
                          StrokeThickness="1"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                </Button>

                <!-- Maximize/Restore button -->
                <Button x:Name="MaximizeRestoreButton"
                        Style="{StaticResource WindowChromeButtonStyle}"
                        Width="46"
                        Height="32"
                        Click="MaximizeRestoreButton_Click">
                    <Path x:Name="MaximizeRestorePath"
                          Data="M0,0 L10,0 L10,10 L0,10 Z"
                          Stroke="{DynamicResource TextSecondaryBrush}"
                          StrokeThickness="1"
                          Fill="Transparent"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                </Button>

                <!-- Close button -->
                <Button x:Name="CloseButton"
                        Style="{StaticResource WindowChromeCloseButtonStyle}"
                        Width="46"
                        Height="32"
                        Click="CloseButton_Click">
                    <Path Data="M0,0 L10,10 M0,10 L10,0"
                          Stroke="{DynamicResource TextSecondaryBrush}"
                          StrokeThickness="1"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
