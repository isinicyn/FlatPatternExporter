<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib"
                    xmlns:local="clr-namespace:FlatPatternExporter">


    <!-- Базовые стили для ячеек -->
    
    <!-- Базовый стиль для индикатора выражения fx -->
    <Style x:Key="BaseExpressionIndicatorStyle" TargetType="TextBlock">
        <Setter Property="Text" Value="fx"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="0,0,3,0"/>
        <Setter Property="FontStyle" Value="Italic"/>
        <Setter Property="FontSize" Value="{StaticResource SmallFontSize}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        <Setter Property="ToolTip" Value="Значение задано выражением (Expression)"/>
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>

    <!-- Стиль для индикатора выражения PartNumber -->
    <Style x:Key="PartNumberExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding PartNumberIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Стили для остальных редактируемых свойств -->
    <Style x:Key="DescriptionExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding DescriptionIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AuthorExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding AuthorIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RevisionExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding RevisionIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TitleExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding TitleIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SubjectExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SubjectIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="KeywordsExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding KeywordsIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CommentsExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CommentsIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CategoryExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CategoryIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ManagerExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding ManagerIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CompanyExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CompanyIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ProjectExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding ProjectIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="StockNumberExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding StockNumberIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CostCenterExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CostCenterIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CheckedByExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CheckedByIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="EngApprovedByExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding EngApprovedByIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="UserStatusExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding UserStatusIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CatalogWebLinkExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CatalogWebLinkIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="VendorExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding VendorIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MfgApprovedByExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding MfgApprovedByIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DesignStatusExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding DesignStatusIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DesignerExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding DesignerIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="EngineerExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding EngineerIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AuthorityExpressionIndicatorStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseExpressionIndicatorStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding AuthorityIsExpression}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <!-- Базовый стиль для всех текстовых ячеек -->
    <Style x:Key="BaseCellStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource StandardFontSize}"/>
        <Setter Property="Padding" Value="{StaticResource StandardPaddingThickness}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>

    <!-- Стандартный стиль для обычных колонок -->
    <Style x:Key="CenteredCellStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseCellStyle}"/>

    <!-- Шаблоны данных -->
    
    <!-- Шаблон для колонки "Изобр. детали" -->
    <DataTemplate x:Key="PartImageTemplate">
        <Image Source="{Binding Preview}" 
               Margin="{StaticResource VerticalHairlineMarginThickness}" 
               HorizontalAlignment="Center" 
               VerticalAlignment="Center"/>
    </DataTemplate>

    <!-- Шаблон для колонки "Изобр. развертки" -->
    <DataTemplate x:Key="DxfImageTemplate">
        <Grid Margin="{StaticResource VerticalHairlineMarginThickness}">
            <Image Source="{Binding DxfPreview}" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"/>
            <TextBlock Text="Нет миниатюры" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="{StaticResource SmallFontSize}" 
                       Foreground="{DynamicResource TextDisabledBrush}"
                       TextWrapping="Wrap"
                       TextAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DxfPreview}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </DataTemplate>

    <!-- Шаблон для колонки "Обозначение" с индикатором развертки -->
    <DataTemplate x:Key="PartNumberWithIndicatorTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Rectangle Grid.Column="0" 
                       Width="3" 
                       HorizontalAlignment="Left" 
                       VerticalAlignment="Stretch" 
                       Margin="{StaticResource LeftTinyMarginThickness}">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="{DynamicResource ErrorBrush}"/>
                        <Setter Property="ToolTip" Value="Развертка отсутствует"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasFlatPattern}" Value="True">
                                <Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/>
                                <Setter Property="ToolTip" Value="Развертка доступна"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
            <TextBlock Grid.Column="1" 
                       Text="{Binding PartNumber}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="2" 
                       Style="{StaticResource PartNumberExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <!-- Универсальные шаблоны для редактируемых свойств с индикаторами выражений -->
    <DataTemplate x:Key="DescriptionWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Description}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource DescriptionExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="AuthorWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Author}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource AuthorExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="RevisionWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Revision}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource RevisionExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TitleWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Title}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource TitleExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="SubjectWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Subject}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource SubjectExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="KeywordsWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Keywords}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource KeywordsExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CommentsWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Comments}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource CommentsExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CategoryWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Category}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource CategoryExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="ManagerWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Manager}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource ManagerExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CompanyWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Company}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource CompanyExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="ProjectWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Project}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource ProjectExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="StockNumberWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding StockNumber}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource StockNumberExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CostCenterWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding CostCenter}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource CostCenterExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CheckedByWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding CheckedBy}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource CheckedByExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="EngApprovedByWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding EngApprovedBy}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource EngApprovedByExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="UserStatusWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding UserStatus}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource UserStatusExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CatalogWebLinkWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding CatalogWebLink}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource CatalogWebLinkExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="VendorWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Vendor}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource VendorExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="MfgApprovedByWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding MfgApprovedBy}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource MfgApprovedByExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="DesignStatusWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding DesignStatus}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource DesignStatusExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="DesignerWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Designer}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource DesignerExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="EngineerWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Engineer}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource EngineerExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="AuthorityWithExpressionTemplate">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding Authority}" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Style="{StaticResource CenteredCellStyle}" 
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" 
                       Style="{StaticResource AuthorityExpressionIndicatorStyle}"/>
        </Grid>
    </DataTemplate>

    <!-- Шаблон для колонки "Количество" с цветовой индикацией и возможностью редактирования -->
    <DataTemplate x:Key="EditableQuantityTemplate">
        <Grid>
            <!-- Отображение в режиме чтения -->
            <TextBlock Text="{Binding Quantity}" x:Name="DisplayTextBlock">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource CenteredCellStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsOverridden}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource QuantityOverriddenBrush}"/>
                            </DataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsOverridden}" Value="False"/>
                                    <Condition Binding="{Binding IsMultiplied}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Foreground" Value="{DynamicResource QuantityMultipliedBrush}"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            
            <!-- Редактирование в режиме редактирования -->
            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalContentAlignment="Center"
                     x:Name="EditTextBox"
                     Visibility="Collapsed"/>
        </Grid>
        
        <DataTemplate.Triggers>
            <!-- Переключение между режимами отображения и редактирования -->
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridCell}, Path=IsEditing}" Value="True">
                <Setter TargetName="DisplayTextBlock" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="EditTextBox" Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!-- Шаблон для колонки статуса обработки -->
    <DataTemplate x:Key="ProcessingStatusTemplate">
        <Border Width="10" Height="10" 
                BorderBrush="{DynamicResource TextDisabledBrush}" 
                BorderThickness="{StaticResource StandardBorderThicknessValue}" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ProcessingStatus}" Value="Success">
                            <Setter Property="Background" Value="{DynamicResource ProcessingStatusSuccessBrush}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ProcessingStatus}" Value="Error">
                            <Setter Property="Background" Value="{DynamicResource ProcessingStatusErrorBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </DataTemplate>

    <!-- Стили DataGrid -->
    <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource EvenRowBrush}" />
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource OddRowBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource HighlightBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource HighlightTextBrush}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource HighlightBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource HighlightTextBrush}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- Стиль для заголовков столбцов -->
    <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="Height" Value="{StaticResource LargeElementHeight}"/>
        <Setter Property="FontSize" Value="{StaticResource StandardFontSize}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="{StaticResource StandardPaddingThickness}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="ContextMenu" Value="{x:Null}"/>
    </Style>

    <!-- Стиль для заголовков строк -->
    <Style x:Key="DataGridRowHeaderStyle" TargetType="DataGridRowHeader">
        <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Width" Value="15"/>
        <Setter Property="ContextMenu" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridRowHeader">
                    <Grid>
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль для ячеек -->
    <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
        <Setter Property="FontSize" Value="{StaticResource StandardFontSize}"/>
        <Setter Property="Padding" Value="{StaticResource StandardPaddingThickness}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GridLinesBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource HighlightTextBrush}"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Базовый стиль для TextBlock внутри DataGrid -->
    <Style x:Key="DataGridTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource StandardFontSize}"/>
        <Setter Property="Padding" Value="{StaticResource SmallPaddingThickness}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>

    <!-- Стиль для фантомного заголовка колонки при перетаскивании -->
    <Style x:Key="PhantomColumnHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource LargeFontSize}"/>
        <Setter Property="Padding" Value="{StaticResource SmallPaddingThickness}"/>
        <Setter Property="Opacity" Value="0.7"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="Background" Value="{DynamicResource StatusBarBackgroundBrush}"/>
        <Setter Property="FontWeight" Value="Normal"/>
    </Style>

    <!-- Стиль для фантомного заголовка в режиме удаления -->
    <Style x:Key="PhantomColumnHeaderDeleteStyle" TargetType="TextBlock" BasedOn="{StaticResource PhantomColumnHeaderStyle}">
        <Setter Property="Background" Value="{DynamicResource ErrorBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource BackgroundPrimaryBrush}"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    
    <!-- SVG иконка таблицы для оверлея -->
    <Geometry x:Key="TableIconGeometry">M3 9v6.8c0 1.12 0 1.68.218 2.108a2 2 0 0 0 .874.874c.427.218.987.218 2.105.218h11.606c1.118 0 1.677 0 2.104-.218.377-.192.683-.498.875-.874.218-.428.218-.986.218-2.104V11M3 9v-.8c0-1.12 0-1.68.218-2.108.192-.377.497-.682.874-.874C4.52 5 5.08 5 6.2 5h6.3M3 9h9.5M6 15h4m5.5-7.5h5M18 5v5</Geometry>

    <!-- Стиль для иконки таблицы -->
    <Style x:Key="TableIconStyle" TargetType="Path">
        <Setter Property="Data" Value="{StaticResource TableIconGeometry}"/>
        <Setter Property="Stroke" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="StrokeThickness" Value="1.5"/>
        <Setter Property="Stretch" Value="Uniform"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Стиль для пустой области справа от заголовков -->
    <Style TargetType="DataGridColumnHeadersPresenter">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeadersPresenter">
                    <Grid>
                        <DataGridColumnHeader x:Name="PART_FillerColumnHeader" 
                                              IsHitTestVisible="False" 
                                              BorderThickness="0,0,0,1"
                                              BorderBrush="{StaticResource BorderLightBrush}"
                                              Background="{DynamicResource SelectionBackgroundBrush}"/>
                        <ItemsPresenter/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
